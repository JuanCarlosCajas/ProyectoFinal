import { returnListPurcharseProducts, chargePurchaseProducts } from "../classes/prueba";

export function initProducts(){
    const listWantedProducts = JSON.parse(localStorage.getItem('products'))
    for(let i = 0; i > listWantedProducts; i++){

        

        //listWantedProducts[i][] = 
    }
}